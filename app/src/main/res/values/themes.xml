<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CreatePlanetApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="backgroundColor">@color/white</item>
    </style>

    <style name="Theme.CreatePlanetApp" parent="Base.Theme.CreatePlanetApp" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/create_planet</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.CreatePlanetApp</item>
    </style>
</resources>